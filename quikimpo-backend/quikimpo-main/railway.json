{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "buildCommand": "pip install -r requirements.txt",
    "startCommand": "gunicorn pos_system.wsgi:application --log-file - --bind 0.0.0.0:$PORT"
  }
}